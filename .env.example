APP_ENV=production
APP_URL=https://puntorosa.ordena.garciacore.com
DB_HOST=db
DB_PORT=3306
DB_DATABASE=ordena
DB_USERNAME=ordena
DB_PASSWORD=cambia_esta_clave
MYSQL_ROOT_PASSWORD=cambia_root
REDIS_HOST=redis

# Geocodificación (OpenStreetMap Nominatim)
# Cambia el user-agent por el nombre de tu app/dominio y un correo real de soporte.
# Nominatim requiere esto para su uso gratuito.
GEOCODING_USER_AGENT=OrdenaInicial/1.0 (contacto@tudominio.com)

# Envío de OTP por SMS
# SMS_PROVIDER=log   -> En desarrollo escribe el código en los logs (sin costo).
# SMS_PROVIDER=twilio -> En producción envía SMS reales (requiere credenciales).
SMS_PROVIDER=log
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=

# Verificación por teléfono (OTP)
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=60
